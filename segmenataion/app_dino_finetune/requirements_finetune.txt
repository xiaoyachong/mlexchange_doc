# requirements_finetune.txt
# For: python finetune.py
#
# Install:
#   pip install torch==2.9.1 torchvision==0.24.1 --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements_finetune.txt

# --- PyTorch (install separately with CUDA index URL, see above) ---
torch==2.9.1
torchvision==0.24.1

# --- lightly_train and its direct deps ---
lightly-train==0.13.2
pytorch-lightning==2.6.0
numpy==2.2.6
Pillow

# --- MLflow (to download checkpoint and register finetuned model) ---
mlflow==2.22.0

# --- Tiled (to read images and masks) ---
tiled[client]

# --- Config / env loading ---
pyyaml
python-dotenv
